#include <windows.h>

int CALLBACK WinMain(
  HINSTANCE hInstance,
  HINSTANCE hPrevInstance,
  LPSTR     lpCmdLine,
  int       nShowCmd
)
{
	MessageBox(0, "its working", "Handmade Hero", MB_OK|MB_ICONINFORMATION);
	return(0);
};




















